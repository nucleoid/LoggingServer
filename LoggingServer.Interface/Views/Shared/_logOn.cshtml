@using LoggingServer.Interface.Controllers
@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@(Html.ActionLink<AccountController>(x => x.ChangePassword(), User.Identity.Name))</strong>!
    [ @(Html.ActionLink<AccountController>(x => x.LogOff(), "Log Off")) ]</text>
}
else
{
    @:[ @(Html.ActionLink<AccountController>(x => x.LogOn(), "Log On")) ]
}
